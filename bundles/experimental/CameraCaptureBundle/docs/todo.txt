* inlet / outlet set minimum maximum ?

* update rate context ?
	kann man die setzen?


* kernel 
	ein hauptinclude file und ein include directory

* verändern der update rate zur laufzeit ?

* get all outlets with one callback and onec structure which holds values and according types, names of the outlet and allows to iterate through it...

* registertonewdata keine exception wenn man auf output hören will den es nicht gibt