leg 2 projekte an. das plugin und eine testapp.
in der testapp beginne damit, das framework zu starten:

System testSystem("TEST");

danach versuche das plugin zu laden

Identifier kinectPlugin = testSystem.loadPlugin("KINECT", path, "MultiKinectOpenNI.dll", "MultiKinectOpenNI");

das geht am leichtesten, wenn outputparameter (mind. einer) im xml file definiert sind. das xml muss so heißen wie der klassenname.
dafür muss die funktion ::getMetadata() im plugin definiert sein.

bin nicht sicher, ob nun parameter gesetzt werden müssen bzw. was es genau mit der setup funktion auf sich hat.

jedenfalls kann man zuletzt das plugin starten...